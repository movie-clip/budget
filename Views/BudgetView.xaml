<UserControl x:Class="HomeCharts.Views.BudgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:viewModels = "clr-namespace:HomeCharts.ViewModels"
             xmlns:budgetViews="clr-namespace:HomeCharts.Views.BudgetViews"
             mc:Ignorable="d"
             d:DesignHeight="600">
    
    <ContentControl.DataContext>
        <viewModels:BudgetViewModel/>
    </ContentControl.DataContext>
    
	<ContentControl.Resources>
		<DataTemplate DataType="{x:Type viewModels:ExpensesViewModel}">
			<budgetViews:ExpensesView />
		</DataTemplate>
		<DataTemplate DataType="{x:Type viewModels:IncomeViewModel}">
			<budgetViews:IncomeView />
		</DataTemplate>
	</ContentControl.Resources>
	
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0"
                Background="#200F53"
                CornerRadius="10">

            <StackPanel Orientation="Horizontal">
                <RadioButton Style="{StaticResource budgetButton}"
							 Width="100" Height="35"
		                    Tag="{StaticResource color15}"
                            IsChecked="True" Command="{Binding ShowExpensesViewCommand}">
					<StackPanel Orientation="Horizontal">
						<fa:IconImage Icon="SquarePollVertical" Style="{StaticResource menuButtonIcon}" HorizontalAlignment="Left" Margin="1,0,20,0"/>
						<TextBlock Text="Expenses" Style="{StaticResource menuButtonText}" HorizontalAlignment="Left" Margin="-14,0,0,0"/>
					</StackPanel>
				</RadioButton>
				<RadioButton Style="{StaticResource budgetButton}"
							 Width="100" Height="35"
		                    Tag="{StaticResource color15}"
                            Command="{Binding ShowIncomeViewCommand}" >
					<StackPanel Orientation="Horizontal">
						<fa:IconImage Icon="SquarePollVertical" Style="{StaticResource menuButtonIcon}" HorizontalAlignment="Left" Margin="1,0,20,0"/>
						<TextBlock Text="Income" Style="{StaticResource menuButtonText}" HorizontalAlignment="Left" Margin="-14,0,0,0"/>
					</StackPanel>
				</RadioButton>
                <Button Content="Import" Height="29" Width="78" Click="OnImportClick"/>
                <Button Content="Save" Height="29" Width="78" Click="OnSaveClick"/>
                <Button Content="Delete all" Height="29" Width="78" Click="OnDeleteClick"/>
            </StackPanel>

		</Border>
        
        <Border Grid.Row="1"
                Background="#200F53"
                CornerRadius="10" 
                Padding="10"
                Margin="0,5,0,0"
                x:Name="budgetView">
            
            <Grid>
                <ContentControl Content="{Binding CurrentChildView}"/>
            </Grid>
            
        </Border>
    </Grid>
</UserControl>
